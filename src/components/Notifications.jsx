import { Box, Typography } from "@mui/material";

const Notification = () => {
  return (
    <Box sx={{ flexGrow: 1, p: 3 }}>
      <Typography variant="h5" fontWeight="bold">Notification</Typography>
    </Box>
  );
};

export default Notification;
